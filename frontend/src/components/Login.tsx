import { Button } from "@mui/material";
import AuthLayout from "../layouts/AuthLayout";

const Login = () => {
  return (
    <AuthLayout>
      <Button>
        Create account
      </Button>
      <span>Login</span>
    </AuthLayout>
  )
}

export default Login;
