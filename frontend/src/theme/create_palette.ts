import type { PaletteOptions } from "@mui/material/styles";
import { primary, secondary, error, info, success, warning } from "./colors";

export const palette:PaletteOptions = {
  divider: "#F2F4F7",
  error,
  info,
  primary,
  success,
  secondary,
  warning,
}
